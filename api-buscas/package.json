{
  "name": "api_tripla_26.03",
  "version": "1.0.0",
  "private": true,
  "description": "LEGADO - substituido por backend-unificado.",
  "main": "index.js",
  "scripts": {
    "start": "npm run dev --prefix ../backend-unificado",
    "dev": "npm run dev --prefix ../backend-unificado",
    "start:ui": "npm run dev --prefix ../backend-unificado",
    "start:ui:win": "npm run dev --prefix ../backend-unificado"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.100.0",
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/node": "^25.5.0",
    "concurrently": "^9.2.1",
    "tsx": "^4.21.0",
    "typescript": "^6.0.2"
  }
}
